services:
  app:
    build: .
    container_name: qrcode-php74
    ports:
      - "8000:8000"
    volumes:
      - ./storage:/app/storage
      - ./public/uploads:/app/public/uploads
    restart: unless-stopped

